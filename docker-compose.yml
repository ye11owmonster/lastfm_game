version: '3.8'

services:
  last-fm-game:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./static:/databases  
    restart: unless-stopped
    env_file:
      - ./.env
    ports:
      - "5000:5000"
